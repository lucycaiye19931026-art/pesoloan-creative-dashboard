services:
  - type: web
    name: pesoloan-creative-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 180
    healthCheckPath: /health
    autoDeploy: true
